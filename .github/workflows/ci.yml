name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint and config checks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Check formatting basics
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          import sys

          allowed_suffixes = {".env", ".example", ".gitignore", ".md", ".yml", ".yaml"}
          allowed_names = {"Makefile"}
          skip_dirs = {".git", "models", "cmd", "internal"}
          failures = []

          for path in sorted(Path(".").rglob("*")):
              if not path.is_file():
                  continue
              if any(part in skip_dirs for part in path.parts):
                  continue
              if path.name not in allowed_names and path.suffix not in allowed_suffixes:
                  continue
              data = path.read_bytes()
              rel = path.as_posix()
              if data and not data.endswith(b"\n"):
                  failures.append(f"{rel}: missing trailing newline")
              for lineno, line in enumerate(data.splitlines(), start=1):
                  if line.rstrip(b" \t") != line:
                      failures.append(f"{rel}:{lineno}: trailing whitespace")

          if failures:
              print("\n".join(failures))
              sys.exit(1)
          PY

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Validate Makefile defaults
        shell: bash
        run: |
          set -euo pipefail
          cp .env.example .env
          make check BACKEND=cpu
          make models >/dev/null
          make schemas
          make check-openai-openapi
          make fmt-check
          make go-vet
          make go-test
          python3 -m py_compile scripts/probe_api_schemas.py scripts/probe_gateway.py scripts/probe_gateway_cancel.py scripts/probe_gateway_capacity.py scripts/probe_gateway_errors.py
          make -n up BACKEND=cpu >/dev/null

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: latest
          args: --timeout=5m

      - name: Validate Compose configs
        shell: bash
        run: |
          set -euo pipefail
          docker compose -f docker-compose.dynamic.yml config >/tmp/compose.dynamic.cpu.yml
          docker compose -f docker-compose.dynamic.yml -f docker-compose.dynamic.vulkan.yml config >/tmp/compose.dynamic.vulkan.yml
          docker compose -f docker-compose.dynamic.yml -f docker-compose.dynamic.cuda.yml config >/tmp/compose.dynamic.cuda.yml
